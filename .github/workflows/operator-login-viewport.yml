name: Operator Login Viewport

on:
  pull_request:
    branches: [main]
    paths:
      - 'apps/web/src/pages/OperatorLoginPage.tsx'
      - 'apps/web/src/operator-login-r3.css'
      - 'apps/web/src/operator-login-viewport.css'
      - 'qa/operator-login-viewport-qa.py'
      - '.github/workflows/operator-login-viewport.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  viewport:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Start web client
        run: |
          npm run dev --workspace=@insurance/web -- --host 127.0.0.1 > .runtime-operator-login-web.log 2>&1 &
          echo $! > .runtime-operator-login-web.pid
          for attempt in $(seq 1 40); do
            curl -fsS http://127.0.0.1:5173/operator/login >/dev/null 2>&1 && exit 0
            if [[ "$attempt" == "40" ]]; then
              cat .runtime-operator-login-web.log || true
              exit 1
            fi
            sleep 1
          done

      - name: Prepare Chrome QA
        run: |
          python -m pip install --disable-pip-version-check selenium==4.35.0
          browser_bin="$(command -v google-chrome || command -v chromium || command -v chromium-browser || true)"
          if [[ -z "$browser_bin" ]]; then
            echo "No supported Chrome/Chromium binary found on runner" >&2
            exit 1
          fi
          echo "BROWSER_BIN=$browser_bin" >> "$GITHUB_ENV"
          "$browser_bin" --version

      - name: Verify login fits target desktop viewports
        env:
          QA_WEB_BASE_URL: http://127.0.0.1:5173
        run: python qa/operator-login-viewport-qa.py

      - name: Upload operator login screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: operator-login-viewport-${{ github.event.pull_request.head.sha || github.sha }}
          path: .qa-artifacts/operator-login/*.png
          if-no-files-found: warn
          retention-days: 14

      - name: Web logs on failure
        if: failure()
        run: cat .runtime-operator-login-web.log || true

      - name: Stop web client
        if: always()
        run: |
          pkill -f 'vite --host 127.0.0.1' 2>/dev/null || true
          [[ -f .runtime-operator-login-web.pid ]] && kill "$(cat .runtime-operator-login-web.pid)" 2>/dev/null || true
